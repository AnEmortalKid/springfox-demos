# springfox-swagger-aggregate

A swagger-ui project that aggregates multiple documents
(TODO rephrase)
 - Demonstrates using swagger groups 
 - Demonstrates manual configuration (without using auto configuration/springfox boot-starter)

```
./gradlew springfox-swagger-aggregate:bootRun
```

// TODO play with swagger-ui config

## Paths
- Swagger UI endpoint: `http://localhost:8080/swagger-ui/`
